# Block

A Block is a piece of code that allows you to interact with the context of an app without needing a route. Since context is specific to routes, a Block is essentially a route handler that can't be directly invoked.
